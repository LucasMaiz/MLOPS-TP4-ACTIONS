pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'fetch code'
                echo 'build code'
            }
        }
        stage('test') {
            steps {
                echo 'running test1'
                echo 'running test2'
            }
        }
    }
}
